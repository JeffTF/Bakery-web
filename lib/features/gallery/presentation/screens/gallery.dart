import 'package:flutter/material.dart';

class CakeGallery extends StatelessWidget {
  const CakeGallery({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}